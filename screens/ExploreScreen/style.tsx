import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    container: {
      flex: 1
    },
    instructions: {
      fontSize: 16,
      textAlign: 'center',
      marginBottom: 20,
    },
});

export default style